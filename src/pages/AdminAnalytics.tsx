// This page has been disabled to allow the build to pass.
// The original code has been removed per user request to "not use admin component and admin ui".
export default function DisabledAnalyticsPage() { return null; }
